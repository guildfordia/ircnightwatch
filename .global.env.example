# Global Project Settings
# The name of the project used for container naming and networking
PROJECT_NAME=ircnightwatch

# Docker networking configuration
# Name of the Docker network to be created for container communication
DOCKER_NETWORK=irc-net

# Shared IRC Settings
# Hostname of the IRC server
IRC_HOST=ngircd
# Port number for IRC server connection (must be a number)
IRC_PORT=6667
# IRC channel to join (must start with #)
IRC_CHANNEL="#nightwatch"

# Optional Settings
# Enable debug mode for verbose logging (true/false)
DEBUG=false
# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Security Notice:
# - Never commit the actual .global.env file to version control
# - Use Docker secrets for sensitive data in production
# - All required variables must be set in the actual .global.env file